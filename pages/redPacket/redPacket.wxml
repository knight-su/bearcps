<view wx:for="{{redPacketList}}">
	<i-card title="{{item.title}}" extra="{{item.extra}}" thumb="{{item.icon}}" data-redpacket="{{item}}" bindtap="skipOtherMiniProgram">
	<view slot="content">内容不错</view>
    <view slot="footer">尾部内容</view>
</i-card>
</view>

